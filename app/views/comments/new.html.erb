<h1>New Comment</h1>
<div class="container-fluid">
  <div class="padded-content">
    <% if user_signed_in? and current_user.is_admin? %>
      <%= render 'form' %>
    <% else redirect_to post_comments_path %>
    <% end %>
    <%= link_to 'Back', post_comments_path %>
  </div>
</div>