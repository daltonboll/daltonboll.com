<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h6>Title</h6>
    <%= f.text_field :title %>
  </div>
  </br>
  <div class="field">
    <h6>Body</h6>
    <%= f.text_area :body %>
  </div>
  </br>
  <div class="actions">
    <%= f.submit class: 'btn btn-default btn-lg pretty-button' %>
  </div>
  </br>
<% end %>
