<h1>Messages#new</h1>
<div class="container-fluid">
  <h1>Contact Me</h1>
  <hr/>
  <div class="row">
    <div class="col-md-6">
      <%= form_for @message do |f| %>
        <% if @message.errors.any? %>
          <div id="error_explanation">
            <h2><%= "#{pluralize(@message.errors.count, "error")} prohibited this message from being sent:" %></h2>
            <ul>
              <% @message.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            <ul>
          </div>
        <% end %>

        <div class="field">
          <p>Name:</p>
          <%= f.text_field :name %>
        </div>

        <div class="field">
          <p>Email:</p>
          <%= f.email_field :email %>
        </div>

        <div class="field">
          <p>Subject:</p>
          <%= f.text_field :subject %>
        </div>

        <div class="field">
          <p>Message:</p>
          <%= f.text_area :content %>
        </div>

        <div class="actions">
          <%= f.submit 'Send', class: 'button' %>
        </div>
      <% end %>
    </div>

    <div class="col-md-6">
      <p>Got a question? Want to let me know about a cool job opportunity or project? Just wanna say hellow? Then fill out this form and hit the "Submit" button, and I'll get back to you as soon as possible!</p>
      <p>You can also contact me through social media:</p>
      <ul>
        <li>Google+</li>
        <li>Twitter</li>
        <li>LinkedIn</li>
        <li>Facebook</li>
        <li>GitHub</li>
      </ul>
    </div>
  </div>
</br>
</br>
</div>