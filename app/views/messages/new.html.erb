<div class="container-fluid">
  <h1>Contact Me</h1>
  <hr/>
  <div class="row">
    <div class="col-md-6">
      <%= form_for @message do |f| %>
        <% if @message.errors.any? %>
          <div id="error_explanation">
            <h2><%= "#{pluralize(@message.errors.count, "error")} prohibited this message from being sent:" %></h2>
            <ul>
              <% @message.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            <ul>
          </div>
        <% end %>

        <div class="field">
          <h4>Name:</h4>
          <%= f.text_field :name, class: "contact-input" %>
        </div>
        </br>
        <div class="field">
          <h4>Email:</h4>
          <%= f.email_field :email, class: "contact-input" %>
        </div>
        </br>
        <div class="field">
          <h4>Subject:</h4>
          <%= f.text_field :subject, class: "contact-input" %>
        </div>
        </br>
        <div class="field">
          <h4>Message:</h4>
          <%= f.text_area :content %>
        </div>
        </br>
        <div class="actions">
          <%= f.submit 'SEND', class: 'btn btn-default btn-lg contact-button' %>
        </div>
      <% end %>
    </div>

    <div class="col-md-6">
      <p>Got a question? Want to let me know about a cool job opportunity or project? Just wanna say hello? Then fill out this form and hit the 'SEND' button, and I'll get back to you as soon as possible!</p>
      <p>You can also contact me through social media:</p>
      <ul>
        <li>Google+</li>
        <li>Twitter</li>
        <li>LinkedIn</li>
        <li>Facebook</li>
        <li>GitHub</li>
      </ul>
    </div>
  </div>
</br>
</br>
</div>